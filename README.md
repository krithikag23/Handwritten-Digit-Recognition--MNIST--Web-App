# ✍️ Handwritten Digit Recognition Web App (MNIST)

This project is a **Handwritten Digit Recognition** web application where the user can **draw a digit (0–9)** on an interactive canvas, and a **Convolutional Neural Network (CNN)** predicts the digit in real-time.  
It is built using **Streamlit**, **TensorFlow/Keras**, and **streamlit-drawable-canvas**.

---

## 🎯 Features
- Draw digits directly in the browser
- CNN trained on the **MNIST dataset**
- Real-time digit prediction
- Runs on **CPU** (no GPU needed)
- Works fully **offline**

---

## 💡 How to Use
1. Draw any digit (0–9) inside the canvas area.
2. Click **Predict**.
3. The model will display the most likely digit 🎉

---

## 🧠 Model Information
- Dataset: **MNIST**
- Input Size: **28×28 grayscale**
- Model Type: **CNN**
- Activation: ReLU + Softmax output
- Loss: Categorical Crossentropy
- Optimizer: Adam

---

## 🚀 Future Enhancements
- Add **Clear Canvas** button
- Add **Confidence Probability Bar Chart**
- Deploy to **Streamlit Cloud / HuggingFace Spaces**
- Support for upload-based handwritten digit images

---



